---
driver:
  name: vagrant

vagrant:
  platforms:
    - name: centos7
      box: centos/7
  providers:
    - name: virtualbox
      type: virtualbox
  instances:
    - name: ansible-docker

ansible:
  requirements_file: requirements.yml
